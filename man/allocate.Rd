% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core.R
\name{allocate}
\alias{allocate}
\title{Allocate into Study Arms}
\usage{
allocate(.flow, variable = NULL, labels = NULL, n = NULL, label = "Randomized")
}
\arguments{
\item{.flow}{A \code{selecta} object.}

\item{variable}{Character string naming the column that defines the arms
(data-driven mode). Ignored in manual mode.}

\item{labels}{A character vector of arm labels. In data-driven mode, this
can be a named vector to relabel factor levels (e.g.,
\code{c(A = "Drug A", B = "Placebo")}). In manual mode, these are the
arm names.}

\item{n}{Integer vector (manual mode). Number of participants allocated to
each arm, in the same order as \code{labels}.}

\item{label}{Character string for the allocation box. Defaults to
\code{"Randomized"}.}
}
\value{
The updated \code{selecta} object.
}
\description{
Splits the enrollment flow into parallel arms (e.g., treatment groups).
Subsequent \code{exclude()} calls apply within each arm independently.
}
\examples{
enroll(n = 400) |>
  exclude("Ineligible", n = 50) |>
  allocate(labels = c("Drug A", "Placebo"), n = c(175, 175))

}
