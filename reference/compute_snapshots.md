# Compute Snapshots at Each Stage

Walks the step list and captures the dataset state at each step,
including both retained and excluded participants.

## Usage

``` r
compute_snapshots(x)
```

## Arguments

- x:

  A `selecta` object.

## Value

A list with `final` and `stages`.
